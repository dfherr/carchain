<div class="col-lg-12 well">
<h2>
  Übersicht über Ihre Anträge
</h2>
  <div class="table-responsive">
    <% if @table_data.empty? %>
      <p> Es liegen keine Anträge vor.</p>
      <p> <%= link_to 'Accept', car_accept_registration_path(1), class: 'btn btn-primary' %></p>
    <% else %>
      <table class="table table-striped table-hover ">
        <thead>
        <tr>
          <th>#</th>
          <th>Referenz</th>
          <th>Halter</th>
          <th>Status</th>
          <th>Datum</th>
          <th></th>
        </tr>
        </thead>
        <tbody>
        <% @table_data.each_with_index do |registration, i| %>
          <tr class="<%= registration[:status][:class] %>">
            <td><%= i+1 %></td>
            <td><span class="hash-value"><%= registration[:reference] %></span></td>
            <td><%= registration[:owner] %></td>
            <td><%= registration[:status][:text] %></td>
            <td><%= registration[:time] %></td>
            <td>
              <%= link_to 'Accept', car_accept_registration_path(registration[:id]), class: 'btn btn-xs btn-primary' %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>
    <% end %>

  </div>
</div>
