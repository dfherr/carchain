<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Carchain</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-2">
      <ul class="nav navbar-nav">
        <% if can? :manage, CarRegistration %>
          <li <%= "class=active" if current_page?(car_registration_path) %>>
            <%= link_to _("Car overview"), car_registration_path %>
          </li>
          <li <%= "class=active" if current_page?(car_registration_register_path) %>>
            <%= link_to _("Register car"), car_registration_register_path %>
          </li>
        <% end %>
        <% if can? :manage, Car::OfficialController %>
          <li <%= "class=active" if current_page?(car_official_path) %>>
            <%= link_to _("Official"), car_official_path %>
          </li>
        <% end %>
        <% if can? :manage, Car::InsuranceController %>
          <li <%= "class=active" if current_page?(car_insurance_search_for_registration_path) %>>
            <%= link_to _("Insurance"), car_insurance_search_for_registration_path %>
          </li>
        <% end %>
        <% if can? :manage, Car::PoliceController %>
          <li <%= "class=active" if current_page?(car_police_search_for_owner_path) %>>
            <%= link_to _("Police"), car_police_search_for_owner_path %>
          </li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if can? :manage, User %>
        <li <%= "class=active" if current_page?(users_roles_path) %>>
          <%= link_to _("Manage roles"), users_roles_path %>
        </li>
        <% end %>
        <% if user_signed_in? %>
            <li <%= "class=active" if current_page?(edit_user_registration_path) %>>
              <%= link_to current_user.email, edit_user_registration_path %>
            </li>
            <li>
              <%= link_to _("Log out"), destroy_user_session_path, method: :delete %>
            </li>
        <% else %>
            <li <%= "class=active" if current_page?(new_user_session_path) %>>
              <%= link_to _("Log in"), new_user_session_path %>
            </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
